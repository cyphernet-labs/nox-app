---

description: "Task list for feature 033 — the server knows its owner"
---

# Tasks: Сервер знает своего владельца

**Input**: Design documents from `/specs/033-server-owner/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md), [data-model.md](./data-model.md), [contracts/](./contracts/)

**Tests**: включены. Их требует сама спека (FR-010, FR-024, SC-004, SC-005, SC-006) и обязательный локальный гейт конституции — CI на паузе, и тесты единственное место, где регрессия ловится.

**Organization**: задачи сгруппированы по пользовательским историям. US2 идёт **раньше** US1, хотя обе P1: отметку в настройках нечем нарисовать, пока признак не доезжает от сервера.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: можно делать параллельно (разные файлы, нет незакрытых зависимостей)
- **[Story]**: US1 / US2 / US3 из [spec.md](./spec.md)

---

## Phase 1: Контракт (блокирует всё)

**Purpose**: Принцип VII — провод правится контрактом первым, до единой строки кода.

- [X] T001 Внести поле `owner` в объект `identity` ответа на приветствие (§3) в `docs/client-backend/protocol/contract-draft.md`, обновив пример кадра
- [X] T002 Внести то же поле в объект `identity` ответа на `pair` (§8A) в `docs/client-backend/protocol/contract-draft.md`, обновив пример кадра
- [X] T003 Записать в §8A правила поля из `specs/033-server-owner/contracts/wire-changes.md`: отвечает про спрашивающего, описывает личность, присутствует всегда без `omitempty`, клиент не выводит владение сам
- [X] T004 Уточнить в `docs/client-backend/protocol/contract-draft.md` §8A определение «сервер забран» — это наличие владельца; отдельной решающей отметки не существует, момент времени сервер помнит, но правила на него не смотрят
- [X] T005 Отметить в `docs/client-backend/protocol/contract-draft.md` (§8A и таблица статусов §8), что `device.list` признаком владения **не** расширяется и вопрос возвращается в фазу 034

**Checkpoint**: контракт описывает провод, которого ещё нет в коде — дальше код догоняет контракт.

---

## Phase 2: Foundational — владелец в хранилище (блокирует все истории)

**Purpose**: понятие владельца в схеме и переключение трёх мест решения с `claimed_at` на владельца. Ни одна история без этого не начинается.

- [X] T006 Добавить колонку `owner_user_id TEXT NULL REFERENCES users(user_id)` в таблицу `server_identity` в `client_backend/migrations/001_init.sql`, правкой **на месте** (пре-релизное правило); в комментарии таблицы записать, что наличие ссылки и есть «сервер забран», а `claimed_at` остаётся информацией для статусной страницы
- [X] T007 Добавить `OwnerUserID string` в `store.ServerIdentity` и прочитать колонку в `readServerIdentity` в `client_backend/internal/store/serverkey.go`
- [X] T008 Добавить в `client_backend/internal/store/serverkey.go` предикат «забран» поверх владельца и функцию простановки владельца внутри переданной транзакции; в комментарии — почему решение идёт от владельца, а не от `claimed_at`
- [X] T009 Добавить `Owner bool` в `store.Identity` в `client_backend/internal/store/identity.go`; в комментарии объяснить отличие от `Created`: `Owner` описывает личность и потому устойчив, `Created` описывает ответ
- [X] T010 Заполнить `Owner` в `ResolveIdentity` в `client_backend/internal/store/identity.go` — сравнением найденной личности с владельцем машины. Это **новое чтение** строки машины в пути приветствия: сегодня `ResolveIdentity` её не читает вовсе. Одним запросом на разрешение, внутри уже существующей транзакции
- [X] T011 В `client_backend/internal/store/pairing.go` в ветке `TokenClaim` заменить чтение `claimed_at` на чтение владельца: сервер занят, если владелец есть **и** устройств больше нуля
- [X] T012 В `client_backend/internal/store/pairing.go` проставлять владельца в той же транзакции, что создаёт личность claim-путём, и **не** трогать владение на пути переприсоединения к существующей личности
- [X] T013 В `client_backend/internal/store/pairing.go` заполнить `Owner` в возвращаемой `Identity` на всех трёх выходах ветки claim: новая личность, переприсоединение, повтор потерянного ответа (`pairedBy`)
- [X] T014 В `client_backend/internal/store/pairing.go` заполнить `Owner` в ветке приглашения устройства — приглашённое устройство принадлежит той же личности, поэтому значение то же, что у неё
- [X] T015 **СОХРАНИТЬ** инлайновую запись `claimed_at` в ветке claim в `client_backend/internal/store/pairing.go` при переписывании T011–T013 — момент «когда забрали» невосстановим (FR-006), а запись стоит ровно в переписываемом месте
- [X] T016 Удалить мёртвую функцию `MarkClaimed` из `client_backend/internal/store/serverkey.go` — она не вызывается ниоткуда с фазы 032 и выглядит как место записи `claimed_at`, которым не является
- [X] T017 В `client_backend/internal/server/server.go` перевести `announceClaim` на владельца: ссылка печатается, пока владельца нет **или** устройств нет
- [X] T018 Расширить `assertIdentitySchema` в `client_backend/internal/server/server.go` проверкой **колонки** `owner_user_id`, а не только наличия таблиц: схема правится на месте, поэтому база прошлой фазы иначе падает сырым `no such column` вместо подготовленного «удалите базу и запустите заново»
- [X] T019 Добавить в `client_backend/internal/server/server.go` предупреждение на старте о хранилище с личностями, но **без** владельца — без идентификатора личности в сообщении (Принцип I); сервер продолжает запуск (FR-011)

**Checkpoint**: `go build ./...` и `gofmt -l .` чисты; владение записано и читается, решения приняты по нему, `claimed_at` по-прежнему пишется, а база прошлой фазы получает подготовленное сообщение вместо сырой ошибки.

---

## Phase 3: US2 — признак владения доезжает до устройства (Priority: P1)

**Goal**: устройство узнаёт о владении от сервера в обоих кадрах и хранит услышанное.

**Independent Test**: два клиента против одного `noxd` — забравший сервер получает признак, приглашённое устройство той же личности получает такой же. Проверяется без единого экрана.

### Сервер — провод

- [X] T020 [US2] Добавить `Owner bool` в `greetingIdentity` в `client_backend/internal/server/handlers.go` с тегом без `omitempty` и комментарием, почему пропавшее `false` недопустимо
- [X] T021 [US2] Добавить `Owner bool` в `identity` (ответ на `pair`) в `client_backend/internal/server/handlers.go`, там же
- [X] T022 [US2] Заполнить признак при сборке ответа на приветствие в `client_backend/internal/server/handlers.go` из `store.Identity.Owner`
- [X] T023 [US2] Заполнить признак при сборке ответа на `pair` в `client_backend/internal/server/pairing.go` из `store.Identity.Owner`

### Сервер — тесты

- [X] T024 [P] [US2] Тест в `client_backend/internal/store/pairing_test.go`: claim на пустом хранилище заводит личность, помечает её владельцем и возвращает `Owner: true`
- [X] T025 [P] [US2] Тест в `client_backend/internal/store/pairing_test.go`: приглашённое устройство даёт `Owner: true` для той же личности — владение принадлежит человеку, а не аппарату
- [X] T026 [P] [US2] Тест в `client_backend/internal/store/identity_test.go`: `ResolveIdentity` владельца возвращает `Owner: true`, а личности без владения — `false`
- [X] T027 [P] [US2] Тест в `client_backend/internal/store/serverkey_test.go`: «забран» считается от владельца — хранилище с `claimed_at`, но без владельца считается незабранным
- [X] T028 [US2] Тест в `client_backend/internal/server/pairing_test.go`: ответ на `pair` несёт владение в том же кадре, и приветствие следом несёт его же
- [X] T029 [US2] Тест в `client_backend/internal/server/pairing_test.go`: поле присутствует в кадре даже со значением `false` — проверять по сырому JSON, а не по разобранной структуре, иначе `omitempty` пройдёт незамеченным
- [X] T030 [US2] Тест в `client_backend/internal/server/pairing_test.go`: **идентификатор владельца в кадр не попадает** (FR-015) — искать `user_id` владельца в сыром JSON обоих ответов и не находить нигде, кроме собственного `identity.id` спрашивающего

### Клиент — данные

- [X] T031 [P] [US2] Добавить `bool? isOwner` в `lib/domain/model/session/server_identity.dart` (разбирается из поля провода `owner`) с комментарием, почему тип нулевой: `null` = «сервер не заявил», и это не то же самое, что «не владелец»
- [X] T032 [P] [US2] Добавить `bool? isOwner` в `lib/domain/model/app/session_model.dart` рядом с `authorId` — **то же имя, что в `ServerIdentity`**: один факт, одно имя
- [X] T033 [US2] Разобрать `owner` из ответа на приветствие в `lib/data/remote/socket/nox_socket_client.dart`
- [X] T034 [US2] Разобрать `owner` из ответа на `pair` в `lib/data/repository/app/auth_repository_impl.dart`
- [X] T035 [US2] Расширить `adoptServerIdentity` параметром `isOwner` в `lib/domain/repository/app/session_repository.dart` и `lib/data/repository/app/session_repository_impl.dart`; хранить в `SharedPreferences` под ключом `session.is_owner`
- [X] T036 [US2] Стирать `session.is_owner` в `clear()` и в `discardSignIn()` в `lib/data/repository/app/session_repository_impl.dart` — несостоявшийся вход не оставляет следа о владении
- [X] T037 [US2] Передавать признак из приветствия в сессию в `_adoptGreeting` в `lib/data/sync/live_session_starter.dart`
- [X] T038 [US2] Передавать признак из ответа на `pair` в сессию в `signIn` в `lib/data/repository/app/auth_repository_impl.dart` — владелец узнаёт о себе сразу, не дожидаясь приветствия

- [X] T039 [US2] Прогнать `make generate` после T031–T032 — freezed-типы `ServerIdentity` и `SessionModel` без него не соберутся, а `.freezed.dart` в репозитории не лежит

### Клиент — тесты

- [X] T040 [P] [US2] Тест в `test/data/repository/app/session_repository_impl_test.dart`: признак сохраняется, переживает перезапуск и стирается выходом
- [X] T041 [P] [US2] Тест в `test/data/remote/socket/nox_socket_client_test.dart`: приветствие без поля даёт `null`, а не `false` — «не заявлено» отличимо от «не владелец»
- [X] T042 [US2] Тест в `test/data/sync/live_session_starter_test.dart`: признак из приветствия доезжает до сессии, и приветствие без сессии его не пишет

- [X] T043 [P] [US2] Тест на журналы (FR-024, SC-006): признак владения не пишется в лог рядом с идентификатором личности — по образцу теста фазы 032, который пинил утечку `FormatException`. Серверная половина в `client_backend/internal/server/`, клиентская в `test/data/sync/`

**Checkpoint**: `go test -race ./...` и `make gate` зелёные; признак доезжает и хранится, экранов ещё нет.

---

## Phase 4: US1 — хозяин видит, что машина его (Priority: P1)

**Goal**: отметка владения в корне настроек на обеих ширинах.

**Independent Test**: чистая установка, claim, выбор имени — отметка стоит рядом с именем; узкая и широкая ширина.

**Depends on**: Phase 3 (нечего рисовать без признака).

- [X] T044 [P] [US1] Добавить ключ `settingsOwnerBadge` в `lib/l10n/app_en.arb` (`Server owner`) и `lib/l10n/app_uk.arb` (`Власник сервера`) — оба ARB обязаны иметь одинаковый набор ключей
- [X] T045 [US1] Добавить необязательный признак владения в `AppIdentityCardWidget` в `lib/presentation/widgets/settings/app_identity_card_widget.dart`; отметка рядом с именем, на токенах и `ColorScheme`, без новых цветовых литералов
- [X] T046 [US1] Провести признак через состояние настроек в `lib/presentation/pages/settings_root_page/bloc/settings_root_state.dart` и `settings_root_bloc.dart`, читая его из сессии
- [X] T047 [US1] Передать признак в карточку из `lib/presentation/pages/settings_root_page/settings_root_page.dart` — на **обеих** ветках раскладки, узкой и широкой
- [X] T048 [P] [US1] Тест в `test/presentation/pages/settings_root_page/bloc/settings_root_bloc_test.dart`: состояние несёт владение из сессии, а «не заявлено» не превращается в «не владелец»
- [X] T049 [P] [US1] Виджет-тест в `test/presentation/widgets/settings/app_identity_card_widget_test.dart`: отметка есть у владельца, отсутствует у не-владельца и отсутствует при «не заявлено». Состояние «не владелец» собирается прямо в тесте — сквозным путём оно в фазе 033 недостижимо (людей ровно один), и это единственное место, где оно проверяется до фазы 034
- [X] T050 [US1] Обновить голдены виджета карточки идентичности в `test/presentation/widgets/settings/` — светлая и тёмная темы
- [X] T051 [US1] Обновить мобильные и **десктопные** голдены корня настроек в `test/presentation/pages/settings_root_page/goldens/` (Принцип VI, FR-021)
- [X] T052 [US1] Обновить голдены оболочки в `test/presentation/widgets/shell/goldens/` — десктопная вкладка настроек рисует ту же карточку

**Checkpoint**: `make golden-verify` зелёный; отметка видна на обеих ширинах.

---

## Phase 5: US3 — владение переживает потерю всех устройств (Priority: P2)

**Goal**: повторный claim возвращает того же человека с тем же владением; второй владелец не заводится.

**Independent Test**: claim → выход → повторный claim по новой ссылке; личность и владение те же, счётчик личностей не вырос.

**Depends on**: Phase 2 (поведение уже заложено там; здесь оно закрепляется тестами и проверяется целиком).

- [X] T053 [P] [US3] Тест в `client_backend/internal/store/pairing_test.go`: повторный claim на сервере без устройств переприсоединяет к существующей личности и **сохраняет** за ней владение; вторая личность не заводится
- [X] T054 [P] [US3] Тест в `client_backend/internal/store/pairing_test.go`: claim на сервере с живым устройством отвергается, владелец не меняется
- [X] T055 [P] [US3] Тест в `client_backend/internal/store/serverkey_test.go`: хранилище с личностями, но без владельца считается незабранным, и владелец по «самой ранней» строке **не** выбирается
- [X] T056 [US3] Тест в `client_backend/internal/server/pairing_test.go`: сквозной путь claim → отзыв последнего устройства → повторный claim; тот же `identity.id`, `owner: true`, `created: false`

**Checkpoint**: `go test -race ./...` зелёный; редкий необратимый путь закреплён.

---

## Phase 6: Polish — документация и живая проверка

**Purpose**: привести доки в соответствие коду (Принцип II) и убедиться, что путь работает не только в тестах.

- [X] T057 [P] Обновить `client_backend/CLAUDE.md`: понятие владельца, «забран» считается от владельца, `claimed_at` больше не решает — в разделе намеренных умолчаний
- [X] T058 [P] Обновить карту файлов в `client_backend/CLAUDE.md` — `serverkey.go` теперь хранит и владельца
- [X] T059 [P] Обновить `docs/design/spec/screens/settings-root.md`: блок имени несёт отметку владения
- [X] T060 [P] Обновить `docs/design/spec/top-level-screens.md` — экран 7.1
- [X] T061 [P] Обновить `docs/blueprints/mobile/` — раздел хранения сессии получает ключ `session.is_owner` (Принцип III: блюпринт возвращается в корректное состояние тем же change-set'ом)
- [X] T062 Отметить фазу 033 в `docs/client-backend/roadmap-stage2.md` как **в работе** (`◐`). Статус «смёржена» ставится при мерже — отдельным решением владельца, не изнутри реализации
- [X] T063 Расширить живой прогон в `test/live/pairing_live_probe.dart` проверкой признака владения после claim
- [X] T064 Прогнать `quickstart.md` вживую против настоящего `noxd` на двух устройствах: US1, US2, US3 и отрицательные проверки
- [X] T065 Финальный гейт: `make gate`, `make golden-verify`, `cd client_backend && go vet ./... && go test -race ./...`

---

## Зависимости

```
Phase 1 (контракт) ──→ Phase 2 (хранилище) ──→ Phase 3 US2 (провод + данные) ──→ Phase 4 US1 (экран)
                                            └──→ Phase 5 US3 (тесты пути)
                                                                              Phase 6 (доки, живой прогон)
```

- **Phase 1 блокирует всё**: Принцип VII не допускает кода раньше контракта.
- **Phase 2 блокирует все истории**: без владельца в хранилище нечего возвращать и нечего показывать.
- **US1 зависит от US2**, хотя обе P1: отметку нечем нарисовать без признака.
- **US3 почти целиком тесты**: поведение заложено в Phase 2, здесь оно закрепляется — но закрепляется обязательно, потому что путь необратимый.
- **Phase 6** идёт последней и целиком.

## Параллельные возможности

| Группа | Задачи | Почему параллельны |
|---|---|---|
| Тесты хранилища | T024–T027 | разные файлы, ни один не правит код |
| Модели клиента | T031, T032 | разные файлы, freezed-типы независимы |
| Тесты клиента | T040, T041, T043 | разные файлы |
| Тесты US3 | T053–T055 | разные файлы, читают уже готовый код |
| Документация | T057–T061 | пять разных файлов |

Внутри `pairing.go` (T011–T015) параллельности нет: один файл, и правки пересекаются.

## Стратегия

**MVP — Phase 1 + Phase 2 + Phase 3 (US2).** Это уже осмысленно: владелец назван, записан и доезжает до устройства, а фазы 034 и 035 получают то, ради чего 033 существует. Отметка в настройках (US1) — то, что делает фазу видимой человеку, и идёт сразу следом.

**US3 не откладывается**, несмотря на P2: путь редкий, но необратимый, а его поведение уже заложено в Phase 2 — оставить его без тестов значит унести незакреплённым ровно то, что дороже всего сломать.

## По итогам реализации

Живой прогон вскрыл дефект, который сама фаза и сделала видимым: сервер печатал при запуске «this server has no owner yet», хотя владелец у него был — не было только устройств. До 033 эти два состояния были неразличимы (обе ветки смотрели на `claimed_at`), и одно сообщение на оба было честным. Теперь они различимы, и сообщение врало тому, кто его читает: машина без владельца вот-вот его получит, а машина без устройств вот-вот впустит своего обратно. Разведено на две строки и закреплено тестом.

## По итогам ПЕРВОГО прохода код-ревью PR #23

11 находок, все подтверждены. Три серверных оказались самыми дорогими, и все три — следствие того, что фаза сделала различимым то, что раньше было слито в одно.

- [X] R1 **Повторный claim выбирал человека по порядку строк.** `onlyUser` (`ORDER BY created_at LIMIT 1`) остался с фазы 032, когда человек на сервере был заведомо один. Теперь у сервера есть владелец — и брать «самого раннего» значит ровно ту догадку, ради удаления которой фаза и делалась: с приходом 034 гость, заведённый раньше владельца, отдал бы новому устройству свою личность и всю переписку, а ответ при этом сказал бы `owner: true`. Переклаим идёт к **владельцу**
- [X] R2 **Хранилище с людьми, но без владельца, снова принимало claim.** Раньше отказ держался на `claimed_at`; сделав владельца единственным определением «забран», я убрал этот второй запор. Частичное восстановление или правка руками — и посторонний получает доступ к чужой истории. Такой claim теперь отвергается: обе доступные догадки (привязать по порядку строк / завести новую личность) хуже отказа и, в отличие от него, необратимы
- [X] R3 **`id.Owner = true` утверждалось после условного UPDATE**, чей `RowsAffected` выбрасывался — при том что комментарий самой функции обещал обратное, а `BurnToken` рядом его проверяет. Владение теперь **перечитывается** после записи: иначе ответ на `pair` обещал бы отметку, которую следующее же приветствие отбирает
- [X] R4 **Небезопасный каст в разборе приветствия вешал сокет навсегда.** `as bool?` бросает на `1` или `"true"`, а `_greet()` ловит только `SocketUnavailableException` и вызывается через `unawaited` — ни teardown, ни ретрая, канал мёртв до перезапуска процесса. Проверка типа вместо каста; заодно исправлен соседний `created`, который был устроен так же
- [X] R5 **`discardSignIn` не убирал `author_id`**, который вход теперь пишет тем же вызовом: несостоявшаяся попытка оставляла указатель на человека с прошлого сервера, и следующий вход помечал бы чужие сообщения своими — та самая опасность, которую `clear()` описывает в собственном комментарии
- [X] R6 **Отметка съедала половину строки имени.** `Flexible` + `Spacer` — два гибких ребёнка, делящих свободное место поровну, поэтому имя укладывалось в половину строки и обрезалось многоточием с пустым местом рядом. У **всех**, с отметкой и без. Заменено на `Wrap`: имя получает полную ширину, а отметка переезжает на свою строку, когда вдвоём не помещаются
- [X] R7 **Отметка не переживала большой масштаб текста.** Украинская строка при `textScale: 2.0` не оставляла имени ничего. Тот же `Wrap` это решает; закреплено тестом на 320dp
- [X] R8 **`isOwner` читался один раз при построении экрана.** Вкладка настроек остаётся смонтированной, поэтому ответ, приехавший позже, не показывался никогда. Появился канал `watchOwnership()` — той же формы, что `watchLabel()`, и с тем же `.skip(1)`, потому что засеянное значение уже читает `initialize`
- [X] R9 Результат `adoptServerIdentity` терялся молча. Не фатально и **не откат**: claim-токен уже сожжён, а откат здесь — это кирпич, от которого фазу 032 лечили. Приветствие следом пишет то же самое, так что сбой самовосстанавливается; исчезать бесследно он не должен
- [X] R10 Ассерция в живой пробе не могла упасть: читала `isOwner` у сессии, про которую строкой выше уже сказано, что она `null`. Проверяет сам ключ в хранилище — и заодно `author_id`
- [X] R11 Оба корпуса дизайна (`nox-mobile-screens`, `nox-desktop-screens`) не знали об отметке, хотя `docs/design/spec/` был обновлён. Правило корпусов из CLAUDE.md: каждый — авторитет для своей ширины

**Проверки после раунда:** `go test -race ./...`, `make gate` = 975, `make golden-verify` = 234, живой прогон целиком.

## По итогам ВТОРОГО прохода код-ревью PR #23

12 находок, все подтверждены. Половина — прямое следствие правок первого прохода: закрывая одну дыру, я открывал соседнюю.

- [X] S1 **Типобезопасность разбора приветствия была сделана наполовину.** Я защитил `created` и `owner`, оставив ровно те же касты на `id`, `label`, `cursor` и трёх полях `limits` — то есть оставив живой ту самую ловушку, про которую написал комментарий. Поля читаются проверкой типа, а `_greet` получил ловлю **всего остального**: она вызывается через `unawaited`, поэтому любое исключение оттуда — это мёртвый канал до перезапуска процесса. Проверки полей покрывают то, что кто-то вспомнил; catch покрывает то, чего не вспомнил никто
- [X] S2 **`discardSignIn` не убирал `label`** — тот же вызов `adoptServerIdentity`, что пишет `author_id`, пишет и имя. Несостоявшийся вход оставлял имя человека с чужого сервера, и оно рисовалось в настройках и обоих аватарах, пока какое-нибудь приветствие его не перезапишет. Плюс `discardSignIn` не оповещал каналы, в отличие от `clear()`, — подписчики продолжали показывать стёртое
- [X] S3 **Предупреждение советовало «переклаймите», а `Pair` ровно этот claim отвергает** — совет, невыполнимый по построению, повторяемый на каждом запуске. Это моё же противоречие из первого раунда. Текст говорит правду (нужна ручная починка), и ссылка в этом состоянии больше не печатается
- [X] S4 **Гонка `initialize` с событием владения.** Обработчик читает владение, ждёт, потом эмитит; канал живёт на своём конкурентном обработчике. Ответ, приехавший во время ожидания, затирался значением, прочитанным до него, — и, поскольку владение больше не меняется, отметка пропадала на весь процесс. Ровно тот промах, ради которого канал и заводился
- [X] S5 Тест «приветствие без сессии не пишет владение» ничего не проверял: `readSession()` возвращает `null` при отсутствии идентификатора независимо от остальных ключей. Убрать сторож в коде — тест остался бы зелёным. Проверяет сами ключи
- [X] S6 Правило владения было скопировано в **четыре** места. Пятое написали бы копированием того из них, что попадётся первым, — и одна забытая проверка на пустую строку раздала бы владение всякой личности с пустым id. Одна функция `ownsServer`
- [X] S7 Ни проверка новой колонки в стороже схемы, ни предупреждение о сироте не имели тестов — то есть опечатка в предикате либо отвергала бы каждую здоровую базу, либо пропускала бы каждую устаревшую. Оба покрыты
- [X] S8 `greetingIdentity` и `identity` стали побайтово одинаковыми, кроме `Created`, с контрактом «без omitempty», поддерживаемым в двух комментариях. Второй встраивает первый: следующее поле нельзя добавить в один кадр и забыть про другой
- [X] S9 **`ON CONFLICT` не перепривязывал `user_id`.** Устройство с уже известным ключом, предъявившее чужое приглашение, получало ответ про одного человека, а строку — про другого; следующее приветствие меняло и автора, и отметку. Тянется с 032, но 033 расширил ложь до роли. Строка и ответ теперь говорят одно
- [X] S10 `ResolveIdentity` начал падать, если строки машины нет: приветствие получало `internal`, а клиент повторяет отказ бесконечно. Отсутствие строки значит «никто не владеет» — это ответ, а не ошибка
- [X] S11 Тест на журналы проходил впустую: два поиска по всему буферу, соединённые «и», удовлетворяются пустым логом. Проверяет **каждую запись** отдельно и падает, если записей нет вовсе
- [X] S12 Переключив тестовую сессию во владельца, я увёл **все** страничные голдены в вариант с отметкой — состояние без неё не было закреплено ни на одной ширине. Свои эталоны на обе ширины

**Проверки после раунда:** `go test -race ./...`, `make gate` = 976, `make golden-verify` = 238, живой прогон целиком.

## По итогам ТРЕТЬЕГО прохода код-ревью PR #23

11 находок. Первая — критическая, и создало её **моё же исправление второго раунда**.

- [X] T1 **Захват чужого устройства.** Во втором раунде я починил расхождение «ответ про одного человека, строка про другого» перепривязкой `user_id` на конфликте. Это закрыло расхождение и открыло дыру: `device_key` — **публичное** значение, оно едет в каждом приветствии и печатается в `device.list`. С приходом 034 любой, кто может выпустить приглашение себе, назвал бы чужой ключ и забрал устройство: оно продолжало бы работать, здоровалось бы уже как захватчик, подписывало бы его именем каждое сообщение и исчезло бы из списка настоящего владельца. Согласовать ответ и строку можно было двумя способами; я выбрал тот, что раздаёт устройства. Теперь чужой ключ **отвергается**, а повторное спаривание своего остаётся разрешённым
- [X] T2 **Ловушка «всего остального» глотала бросок из `onUnauthenticated`** и отменяла решение «не повторять»: фаза возвращалась с `unsupported` на `disconnected`, и отозванное устройство начинало долбиться в сервер, который будет отказывать всегда. Колбэк обёрнут своим try/catch — как сосед сорока строками ниже
- [X] T3 **Курсор подменялся нулём вместо переподключения** — вопреки комментарию, который я к этой строке и написал. `since >= 0` истинно всегда, поэтому клиент объявлял себя догнанным **до** единого кадра replay, а на первом приветствии сохранял ноль. Единственное поле, отвечающее за корректность replay, было единственным, деградировавшим молча. Теперь это переподключение
- [X] T4 Повтор брал личность из строки устройства, а `Created` — из типа токена. Разойтись они могли только через захват из T1, но связь между половинами ответа не должна держаться на отсутствии другой дыры: join требует, чтобы токен и устройство указывали на одного человека
- [X] T5 В ответе на `pair` `id` и `label` остались сырыми кастами, хотя `owner` рядом я проверил. Здесь ставка выше, чем в приветствии: бросок происходит **после** того, как сервер зафиксировал спаривание и сжёг одноразовый claim, — ту же ссылку предъявить уже нельзя
- [X] T6 Ловушка логировала только тип. Добавлено сообщение с местом; **само значение по-прежнему не логируется** — `TypeError` цитирует его, а значением здесь бывает имя человека, и Принцип I держит имена вне журналов независимо от того, ехали ли они по проводу
- [X] T7 `watchOwnership` сеял значение и **потом** подписывался: событие, попавшее в это окно, терялось без replay — ровно тот промах, ради которого канал заводился. `BehaviorSubject` вместо broadcast-контроллера
- [X] T8 `CountUsers` помечен «test-support», хотя стал зависимостью запуска: чистка, доверившаяся комментарию, сломала бы старт
- [X] T9 Правки корпусов добавили строки, противоречащие оставшимся: состояние `id-shown`, «masked ID» в анатомии, QR-диалог и подпись к аккаунт-QR — всё это код давно не рисует (`revealable: false` на обеих ширинах). Плюс новая строка `Server owner` не была внесена в Copy (EN) ни в одном корпусе
- [X] T10 Статус §8.1 контракта называл `identity.owner` фазой 033, а в колонке статуса стояла только 032. Роадмап остаётся `◐` — фаза не смёржена, и ставить «смёржена» изнутри реализации нельзя (находка I2 первого раунда), — но теперь прямо говорит, что код готов и ждёт мержа
- [X] T11 Бейдж переспрашивал `Theme.of(context)`, хотя единственный вызывающий уже разрешил тему двумя строками выше

**Проверки после раунда:** `go test -race ./...`, `make gate` = 977, `make golden-verify` = 238, живой прогон целиком.

## По итогам ЧЕТВЁРТОГО прохода код-ревью PR #23

10 находок. Две первые — критические, и первая из них тянется с фазы 032: я её не создал, но расширил полезную нагрузку, добавив в неё владение.

- [X] U1 **Погашенный claim-токен отдавал личность любому известному ключу.** Воспроизведено пробой: claim токеном `t1` с `dev-a`, затем `dev-b` входит обычным приглашением — и `Pair(t1, "dev-b")` возвращает `{UserID, Label, Owner: true, Created: true}` без ошибки. Связать токен с устройством через таблицу людей нельзя: claim-токен не называет никого. При этом `device_key` **публичен** (едет в каждом приветствии, печатается в `device.list`), токен лежит в логе сервера между перезапусками, а `pair` — единственная команда без подписи. Токен теперь помнит, **кто** его потратил (`used_by`), и повтор отвечает только ему
- [X] U2 **Повтор пере-claim'а заявлял `created: true`**, хотя первый ответ сказал `false`: исход перевыводился из типа токена. Клиент по такому ответу ведёт вернувшегося владельца на экран выбора имени, и введённое имя затирает его собственное. Токен теперь помнит и **исход** (`created_person`) — повтор отвечает тем, что случилось, а не тем, что следует из вида токена
- [X] U3 Вход писал `authorId`/`label` из ответа `pair` **без проверки на пустоту**, в отличие от пути приветствия. Нечитаемое поле деградирует в `''`, а `resolveIdentity` считает `''` отсутствующим и откатывается на идентификатор входа — где с 032 лежит токен спаривания. Own/other-разметка перестаёт совпадать, и каждое своё сообщение возвращается чужим
- [X] U4 У `watchOwnership` было **три разных контракта**: документация обещала свежий single-subscription поток, реализация возвращала общий broadcast-субъект, а фейк закрывался после одного события. Документация описывает то, что есть
- [X] U5 Отказ claim'а на хранилище с людьми без владельца оставлял его невосстановимым, а достижимо оно частичным восстановлением — при котором `EnsureServerIdentity` **молча выдавал новый ключ машины**, ломая пиннинг всем спаренным устройствам. Ключ такому хранилищу больше не выдаётся: сообщение говорит доделать восстановление
- [X] U6 Владение читалось из **двух** источников, а гонку между ними судил флаг. Канал стал единственным источником — гонка исчезла вместе с флагом и тернарником. Общая тестовая сессия при этом перестала заявлять владение: фикстура, которая его объявляет, раздаёт бейдж всем, кто про него не спрашивал
- [X] U7 Правило «люди без владельца» было записано **тремя** разными способами, расходившимися в трактовке отсутствующей строки машины, — в фазе, чей смысл в том, чтобы не хранить один факт дважды. Один предикат `OwnerlessWithPeople`
- [X] U8 `setOwner` выбрасывал `RowsAffected`, из-за чего `Pair` читал строку машины **третий раз** за транзакцию, чтобы узнать, сработала ли его собственная запись. Записывающий отвечает за себя сам
- [X] U9 Комментарий отправлял читателя к функции `claimedByAnother`, которой в дереве нет. В коде, где обоснования несут нагрузку, ссылка на несуществующее имя отправляет искать правило, у которого нет названия
- [X] U10 Фейк закрывал поток после одного события, поэтому поведение, ради которого канал заведён — ответ, пришедший после построения экрана, — было через него недостижимо. Фейк ведёт себя как настоящий канал, и путь закрыт тестом страницы

**Проверки после раунда:** `go test -race ./...`, `make gate` = 978, `make golden-verify` = 238, живой прогон целиком.

## По итогам ПЯТОГО прохода код-ревью PR #23

12 находок. Первая критическая, четвёртая — нарушение Принципа VII, внесённое мной же в четвёртом раунде.

- [X] V1 **Владельца можно было запереть снаружи его собственной машины.** Ворота пере-claim'а считали **все** устройства сервера, а не устройства владельца. С приходом 034 достаточно приглашённого гостя: владелец выходит с последнего своего устройства, гостевое остаётся работать — и claim-ссылка отвергается навсегда. Выпустить приглашение владельцу не с чего, восстановление за Q16. Считаются устройства владельца
- [X] V2 **Общая ловушка могла отменить терминальные решения.** Обёрнут был только колбэк, а `await _teardown(...)` перед ним — нет: сбой закрытия сокета (а он возможен на любой платформе) проваливался в catch-all, который ставит `disconnected` и запускает ретрай. Отозванное устройство уходило в вечный цикл отказов — ровно то, что ветка предотвращает. Обёрнуты обе терминальные ветки целиком, и обе остаются терминальными
- [X] V3 **Смена мира сервера не стирала признак владения.** `forgetAuthorId` убирал только author-id; бейдж переживал пересборку хранилища и заявлял роль на машине, которая её не давала, — до тех пор, пока какой-нибудь отказ не вызовет выход. Все прочие места, стирающие author-id, я расширил, это пропустил
- [X] V4 **Контракт остался с правилом, которое код перестал исполнять.** §8A говорил «`created` считается от типа токена» и прямо называл отвергнутым правило «считать от того, завелась ли строка» — а мой четвёртый раунд ввёл третье: исход **записывается** при трате токена. Контракт — закон (Принцип VII), и вторая реализация, написанная по нему, повела бы вернувшегося владельца на выбор имени. §8A описывает записанный исход, обязательность «того же ключа» и подсчёт устройств владельца
- [X] V5 Сторож схемы объявлял «каждая колонка последующей фазы названа здесь», не называя двух колонок, добавленных **тем же коммитом**. Список стал таблицей, расширяется одной строкой
- [X] V6 `warnOwnerlessStore` обещал «сервер всё равно стартует», но для одного из двух состояний, которые он покрывал (люди есть, строки машины нет), запуск прерывается — и оператор получал два разных совета на одну ситуацию, причём первый невыполним: владельца некуда записать. Состояния разведены
- [X] V7 Вход **выдумывал успех** при пустом author-id в ответе `pair`: ничего не писалось и ничего не логировалось. Более тяжёлый из двух сбоев был единственным беззвучным
- [X] V8 Документация `CountUsers` называла вызывающего, которого нет, а `OwnerlessWithPeople` писал тот же запрос заново — четвёртая копия `SELECT COUNT(1) FROM users`
- [X] V9 `deviceOwnerOf` дублировал `Store.DeviceOwner` с другим признаком «не найдено». Разойтись они могли ровно на том случае, где это важно: один вызывающий — отзыв своих устройств, другой — отказ в захвате чужого. Одно определение
- [X] V10 `announceClaim` перечитывал владение, которое уже держал: предикат вычислялся дважды за запуск, против хранилища, которое между чтениями мог изменить другой коннект
- [X] V11 Комментарий внутри `_onInitialize` описывал уступку каналу, которой в обработчике больше нет
- [X] V12 Ловушка логировала одно событие дважды с одной и той же нагрузкой

**Проверки после раунда:** `go test -race ./...`, `make gate` = 979, `make golden-verify` = 238, живой прогон целиком.

## По итогам ШЕСТОГО прохода код-ревью PR #23

12 находок. Первая — зеркало моего же исправления пятого раунда.

- [X] W1 **Правило одно, предикатов два.** В пятом раунде я научил `Pair` считать устройства **владельца**, а вывод запуска остался считать все подряд. Итог противоположен исходной ошибке и не лучше: у забранного сервера, чей владелец вышел, при живом гостевом устройстве ссылка не печатается **никогда** — при том что `Pair` этот claim принял бы. Владелец заперт снаружи без возможности получить ссылку. Один предикат `OwnerCanStillGetIn`, который читают оба места
- [X] W2 Ловля вокруг ветки `unauthenticated` накрывала teardown **вместе** с колбэком: упавшее закрытие сокета пропускало уведомление, и приложение продолжало показывать вошедшего человека поверх мёртвого канала. Teardown и колбэк — в разных охранах
- [X] W3 Ещё два `await _teardown(...)` — в ветке `SocketUnavailableException` и в самой ловле-всего — оставались незащищёнными, а бросок **из** catch-ветки соседними не ловится и уходит из `_greet`, который зовут через `unawaited`. Один `_teardownQuietly`, через который идут все четыре
- [X] W4 **Повтор разрешал личность через ТЕКУЩУЮ привязку устройства**, а исход брал из записи токена. Ключ, вернувшийся к человеку после выхода и повторного спаривания, получал старый `created: true` — и вёл уже названного человека на выбор имени. Токен теперь помнит и **кого** он завёл; повтор ничего не перевыводит
- [X] W5 Курсор-дробное (`1042.0` после любого JSON-перекодировщика) стал **вечным** ретраем: соседние поля того же кадра при такой же поломке деградируют, а этот — нет. Принимается любое число
- [X] W6 `OwnerlessWithPeople` считался дважды за запуск, в двух местах, которым обязано совпадать. Считается один раз в `Run` и передаётся
- [X] W7 Блок принятия личности строил **фиктивный успех**, чтобы следующему `if` было что проверять: «ничего не пробовали» становилось неотличимо от «запись прошла». Плюс один и тот же комментарий стоял дважды
- [X] W8 `SessionModel.isOwner` **не читал никто** в проде — четвёртое представление одного факта, которое ничто не поддерживает в актуальном виде. Убрано: путь остался один — prefs → `watchOwnership` → блок
- [X] W9 Сторож схемы получил колонку `paired_user_id`. Корневое решение — отпечаток схемы вместо ручного списка — записано как долг: список по построению будет забыт
- [X] W10 `OwnerlessWithPeople` и подсчёт устройств читали два отдельных запроса: между ними мог зафиксироваться claim, и здоровое хранилище объявлялось сиротой. Оба значения читаются в одной транзакции
- [X] W11 **Бейдж исчезал на время переименования**: ветка редактирования возвращала одно поле ввода. Владение к правке имени отношения не имеет, и мигание объяснить человеку нечем
- [X] W12 Импорт `rxdart` встал между двумя `package:nox_app/...`

**Проверки после раунда:** `go test -race ./...`, `make gate` = 980, `make golden-verify` = 238, живой прогон целиком.
