<!--
  SOLUTION TEMPLATE — the DETAILED working doc (the preview brief is its short summary).
  Meant to be edited across rounds and later fed into Spec Kit or a protocol spec.

  PHASE RULE: this project has no implementation of the backend/protocol/transport yet. Describe THE SOLUTION —
  parts, responsibilities, interactions, formats, decisions, corner cases. Do NOT write a file-by-file change map,
  code snippets, migration plans, or test plans for code that does not exist.

  Ground every claim in a project document (docs/research, docs/protocol, docs/design/spec, the register) or in
  the repo with a real `path:line`. Never invent a path or a number. Keep unknowns as explicit open questions.
  Formats must be concrete: not «токен», but «16 байт, одноразовый, TTL 15 минут».
  Delete these guidance comments when filling.
-->
# <Тема> — Solution (рабочая документация)

**Slug:** `<slug>` · **Обновлён:** <дата> · **Режим:** <fresh / update> · **Статус:** <черновик / на ревью / принято>

## 0. Что прочитано

<Фактически прочитанные источники: документы проекта (первичны), репозиторий (со ссылками path:line), внешние
источники (только если реально фетчились). Отдельно — чего НЕ нашлось и что из-за этого осталось открытым.>

## 1. Задача

<2–5 предложений: что решаем, в каких границах, какое ограничение считаем неснимаемым. Без «как» — только «что».>

## 2. Части решения

<Компоненты и зона ответственности каждого. Одна строка на компонент. Обязательно помечай, что уже существует в
репозитории, а чего ещё нет — это разные вещи для читателя.>

| Часть | За что отвечает | Есть сейчас? |
|---|---|---|
| `<имя>` | <…> | 🟢 есть / 🟡 частично / 🔴 нет |

## 3. Взаимодействие

<Кто с кем разговаривает, в какую сторону, по какому каналу. Это основа для диаграмм в брифе. Отдельно назови,
какие связи ОТСУТСТВУЮТ намеренно (например «приложение не знает про X») — читатель почти всегда предполагает
лишние стрелки.>

## 4. Контракты и форматы

<Конкретика: формат ссылки, структура payload с размерами полей, имена команд и событий, коды ошибок, TTL.
Числа и байты, а не намерения. Если формат ещё не решён — так и напиши, и вынеси в §7.>

## 5. Решения и отклонённые альтернативы

| Решение | Выбрали | Отклонили | Почему |
|---|---|---|---|
| <…> | <…> | <…> | <…> |

<Для каждого решения — что должно измениться в мире, чтобы его стоило пересмотреть. Решение без условия
пересмотра — это догма, а не решение.>

## 6. Пограничные случаи

<Что происходит при отказе, повторе, смене адреса/устройства/сети, при действиях пользователя не по сценарию,
при гонке. Секция, ради которой документ читают второй раз. Если случай не покрыт — честно скажи «не решено».>

## 7. Открытые вопросы и блокеры

<🔴 блокер (нельзя двигаться) · 🟡 открытый вопрос · 🟢 принятое допущение. Где есть вопрос в реестре
`docs/research/open-questions.md` — ссылайся по id (Q1, Q7…), не дублируй содержание.>

| | Вопрос / блокер | На ком / что нужно | Реестр |
|---|---|---|---|
| 🔴 | <…> | <…> | <Q#> |

## 8. Вне скоупа

<Что этот документ намеренно не покрывает и где это живёт вместо него.>

## 9. Ссылки и changelog

- **Бриф:** `<slug>-preview.md` / `.pdf` (в этой же папке).
- **Источники:** <документы проекта, файлы репозитория, внешние ссылки>.
- **Changelog:** <дата — что изменилось в этом раунде и почему>.
