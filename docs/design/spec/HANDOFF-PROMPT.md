# Промпт для Claude Design (Figma) — сборка макетов NOX

> Как использовать: добавь всю папку `docs/design/` в контекст Claude Design (Figma) и **вставь в чат текст из блока «ПРОМПТ» ниже** (от начала до конца). Остальные секции файла — пояснения и чек-лист для тебя; их вставлять не обязательно, но можно.

---

## ПРОМПТ (вставлять это)

```
Ты — продуктовый дизайнер. Собери в Figma полный набор высокоточных макетов мобильного приложения NOX по приложенной документации (папка docs/design/). Это секьюрный мессенджер на Flutter + Material Design 3, для iOS и Android, со светлой и тёмной темами.

ИСТОЧНИК ИСТИНЫ — приложенные файлы. Читай в порядке:
1. overview.md — продуктовая модель, все принятые решения, «Глобальные UI-конвенции».
2. design-system.md — цвет (light+dark), типографика, форма, elevation, отступы, иконки, токены компонентов. Цвет извлечён из логотипа (assets/logo-reference.png), seed = бирюза #12B4B4.
3. top-level-screens.md — карта экранов и потоков.
4. screens/*.md — по файлу на экран: лейаут, состояния, взаимодействия, M3-компоненты, точный микрокопирайт, принятые решения.

ПОРЯДОК РАБОТЫ:
ФАЗА 1 — Foundations. Создай переменные/стили из design-system.md: цветовые переменные для двух режимов (Light §2.2 и Dark §2.3, все роли ColorScheme), текстовые стили (§3 type scale + mono для ID + wordmark), радиусы (§4), elevation (§5), сетка/отступы 4dp (§6). Цвет можно сгенерировать из seed #12B4B4 в Material Theme Builder — значения в §2.2/§2.3 должны совпасть.
ФАЗА 2 — Components. Собери переиспользуемые M3-компоненты из §9 design-system.md: нижняя панель + docked FAB «+» (4.1), message bubble (свой/чужой, со статус-иконками), элемент списка чатов (с аватаром и unread-бейджем), identity card, поля/кнопки/SearchBar/SegmentedButton/SwitchListTile/RadioListTile, progress-индикаторы, file-chip, composer, QR-overlay, QR bottom-sheet, SnackBar/MaterialBanner/AlertDialog, generated-avatar (палитра §2.5). Используй варианты/состояния. На экранах ставь ИНСТАНСЫ, не детачь.
ФАЗА 3 — Screens. Построй 17 экранов (список ниже), каждый — в Light И Dark. Размер фрейма — стандартный мобильный (например 393×852). Бери цвета/текст/отступы строго из токенов и спеков, не хардкодь.

ЭКРАНЫ (id — файл): 1.1 splash, 2.1 login, 2.2 qr-scan, 2.3 set-username, 3.1 error, 4.1 tab-bar-shell, 5.1 chats-list, 5.2 chat, 5.3 file-view, 5.4 chat-card, 6.1 create-chat, 7.1 settings-root, 7.2 notifications, 7.3 appearance, 7.4 language, 7.6 terms, 7.7 about. (7.5 Support — вне scope, не делай.)
Для экранов со значимыми состояниями отрисуй их отдельными фреймами/вариантами: loading / empty / error там, где они описаны в спеке экрана (особенно 5.1, 5.2, 2.3, 6.1, 3.1, 5.3).

ЖЁСТКИЕ ПРАВИЛА:
- Весь UI-текст — на АНГЛИЙСКОМ, ДОСЛОВНО из таблиц «Микрокопирайт» каждого экрана. Не переводи, не переписывай, ничего не придумывай.
- Имя продукта — NOX (все заглавные). Единица общения — «chat». Идентификатор в UI — Your ID. Кнопки входа/выхода — Sign in / Log out.
- Две темы обязательны. Два намеренных исключения из темизации: фон splash — всегда тёмный brand/canvas-dark #0C2424; поверхность QR-кода в 7.1 — всегда светлая #FFFFFF (иначе не сканируется в dark).
- Модель — одно открытое общее пространство (все чаты видны всем, нет прав, нет удаления). НЕ добавляй фич, которых нет в спеках: нет delivered/read-квитанций (только sent/pending/error), нет per-chat настроек, нет звонков/историй/реакций.
- Соблюдай токены: фон bubble «свой» = primaryContainer, «чужой» = surfaceContainerHigh; unread-бейдж = primary, cap 99+; маска ID = 8 точек; и т. д. (см. §9).
- Не выдумывай значения. Если в документации чего-то реально не хватает — НЕ угадывай: перечисли это списком «нужны уточнения» и продолжай с остальным.

АССЕТЫ (ещё не нарисованы — используй плейсхолдеры, не выдумывай финал):
- Логотип: используй assets/logo-reference.png как мудборд/плейсхолдер марки на splash; финальный SVG будет позже.
- Иллюстрации empty-state (chats/messages/files): пока — плейсхолдер-боксы в стиле §10 (тонкий контур + brand/teal + brand/gold).
- App icon — не требуется в этом наборе.

РЕЗУЛЬТАТ — Figma-файл со страницами:
- «Foundations» (цветовые переменные light/dark, текстовые стили, сетка, elevation, иконки);
- «Components» (компоненты из §9 с вариантами/состояниями);
- «Screens» (17 экранов × Light/Dark, сгруппированы по потокам: Onboarding 1.1–3.1, Shell+Chats 4.1/5.x/6.1, Settings 7.x).

По завершении дай короткий список: что собрано, какие плейсхолдеры использованы, и «нужны уточнения» (если есть).
```

---

## Пояснения для тебя (не обязательно вставлять)

### Что в папке

| Файл | Роль |
|---|---|
| `README.md` | индекс папки |
| `overview.md` | продуктовая модель + все принятые решения + глобальные UI-конвенции (аватары, иконки файлов, уровни ошибок, форматы времени, offline) |
| `top-level-screens.md` | карта экранов и потоки |
| `design-system.md` | дизайн-токены: цвет (из логотипа), типографика, форма, elevation, отступы, движение, иконки, токены компонентов §9 |
| `screens/*.md` | 18 файлов-спецификаций (17 экранов в scope + `support.md` — заглушка 7.5, вне scope) |
| `assets/logo-reference.png` | референс логотипа (источник палитры) |

### Почему промпт построен «foundations → components → screens»

Так макеты получаются консистентными: цвета и компоненты определяются один раз как переменные/библиотека, экраны — собираются из них. Это совпадает с тем, как устроена `design-system.md` (§2 цвет → §9 компоненты) и как Figma-инструмент строит дизайн-систему перед экранами.

### Чек-лист приёмки (по чему проверять результат)

- [ ] Присутствуют все 17 in-scope экранов, каждый в Light и Dark.
- [ ] Весь текст совпадает с таблицами «Микрокопирайт» (английский, дословно).
- [ ] Компоненты переиспользованы инстансами; цвета через переменные, не хардкод-hex.
- [ ] Splash — тёмный фон; QR-поверхность в 7.1 — белая в обеих темах.
- [ ] Нижняя панель: docked FAB «+» с вырезом, табы Chats/Settings (иконка+подпись).
- [ ] Состояния loading/empty/error отрисованы там, где описаны.
- [ ] Нет фич вне scope (квитанций read/delivered, звонков, реакций, Support).
- [ ] Есть список «нужны уточнения», если инструмент что-то не нашёл.

### Если хочешь дробить работу

Документация самодостаточна для пофазной сборки. Можно гонять Claude Design по одному потоку за раз: сначала «Foundations + Components», затем «Onboarding (1.1–3.1)», «Shell + Chats (4.1, 5.x, 6.1)», «Settings (7.x)». Каждый поток ссылается на ту же дизайн-систему — консистентность сохранится.
